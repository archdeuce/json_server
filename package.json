{
  "name": "json_server",
  "version": "0.1.0",
  "private": false,
  "dependencies": {
    "json-server": "^0.16.3"
  },
  "scripts": {
    "server": "json-server --delay 300 --port 4000 --watch db.json"
  },
  "devDependencies": {}
}
